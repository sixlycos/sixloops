interface:
  display_name: "SixLoops"
  short_description: "Design reusable goal loops for agent work"
  default_prompt: "Use SixLoops to design the first useful goal loop for this repo. If I give a fresh goal, create GOAL/STATE/TEAM artifacts; if I ask for history, mine local sessions. Keep verifier, state, stop condition, and human gate explicit."
